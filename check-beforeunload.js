const lines=require("fs").readFileSync("src/components/editor/panel-editor.tsx","utf8").split("\n");for(let i=0;i<lines.length;i++){if(lines[i].includes("beforeunload")||lines[i].includes("beforeUnload")){console.log((i+1).toString().padStart(4,"0"),lines[i].substring(0,180));}}
