const fs=require("fs");const files=["src/app/api/ai/generate-design/route.ts","src/app/api/ai/generate-copy/route.ts","src/app/api/ai/review-design/route.ts",".env",".env.local"];files.forEach(f=>{try{const code=fs.readFileSync(f,"utf8");console.log("=== "+f+" ===");console.log("Size:",code.length);if(f.includes(".env")){const lines=code.split("\n");lines.forEach(l=>{if(l.includes("OPENAI")||l.includes("API_KEY")||l.includes("openai"))console.log(l.replace(/=.{5,}/,"=***"));});}else{const lines=code.split("\n");for(let i=0;i<Math.min(lines.length,40);i++)console.log((i+1).toString().padStart(4,"0"),lines[i]);}}catch(e){console.log("=== "+f+" === NOT FOUND");}});
