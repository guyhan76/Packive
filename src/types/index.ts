// Early Access 사전 가입
export interface EarlyAccessSignup {
  id?: string
  email: string
  company_name?: string
  box_description?: string // "어떤 패키지를 만드시나요?"
  country?: string
  created_at?: string
}
