const fs=require("fs");const file="src/components/editor/panel-editor.tsx";let lines=fs.readFileSync(file,"utf8").split("\n");console.log("Start:",lines.length);let removed=0;for(let i=lines.length-1;i>=0;i--){const l=lines[i];if(l.includes("inspirePrompt")||l.includes("inspireImage")||l.includes("inspireLoading")||l.includes("generate-design")||l.includes("applyInspire")||l.includes("handleAiInspire")){console.log("DEL "+(i+1)+":",l.substring(0,120));lines.splice(i,1);removed++;}}console.log("Removed:",removed);const code=lines.join("\n");const ob=(code.match(/\{/g)||[]).length;const cb=(code.match(/\}/g)||[]).length;console.log("Lines:",lines.length,"| diff:",ob-cb);fs.writeFileSync(file,code,"utf8");
