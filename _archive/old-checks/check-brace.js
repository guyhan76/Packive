const code=require("fs").readFileSync("src/components/editor/panel-editor.tsx","utf8");const lines=code.split("\n");let depth=0;for(let i=0;i<lines.length;i++){const o=(lines[i].match(/\{/g)||[]).length;const c=(lines[i].match(/\}/g)||[]).length;depth+=o-c;if(depth<0){console.log("NEGATIVE at line "+(i+1)+": depth="+depth+" | "+lines[i].substring(0,80));break;}}console.log("Final depth:",depth);
