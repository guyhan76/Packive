const lines=require("fs").readFileSync("src/components/editor/panel-editor.tsx","utf8").split("\n");let inTemplates=false;for(let i=0;i<lines.length;i++){if(lines[i].includes("const templates")&&(lines[i].includes("DesignTemplate")||lines[i].includes("="))){inTemplates=true;}if(inTemplates){console.log((i+1).toString().padStart(4,"0"),lines[i]);if(lines[i].includes("];")&&inTemplates){break;}}}
