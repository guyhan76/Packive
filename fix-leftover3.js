const fs=require("fs");const file="src/components/editor/panel-editor.tsx";let lines=fs.readFileSync(file,"utf8").split("\n");let removed=0;for(let i=lines.length-1;i>=0;i--){if(lines[i].includes("setInspireLoading")||lines[i].includes("inspirePrompt, panelName")||lines[i].includes("inspireImage")||lines[i].includes("inspireLoading")){console.log("Remove line "+(i+1)+":",lines[i].substring(0,100));lines.splice(i,1);removed++;}}console.log("Removed:",removed);const code=lines.join("\n");const ob=(code.match(/\{/g)||[]).length;const cb=(code.match(/\}/g)||[]).length;console.log("Lines:",lines.length,"| diff:",ob-cb);fs.writeFileSync(file,code,"utf8");
