const fs=require("fs");const files=["src/app/layout.tsx","src/app/page.tsx","src/components/layout/header.tsx"];files.forEach(f=>{try{const code=fs.readFileSync(f,"utf8");const lines=code.split("\n");for(let i=0;i<lines.length;i++){if(lines[i].includes("Header")||lines[i].includes("LanguageSelector")||lines[i].includes("header")){console.log(f+":"+(i+1)+": "+lines[i].substring(0,150));}}}catch(e){console.log(f+": NOT FOUND");}});
