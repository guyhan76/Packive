const fs=require("fs");try{const code=fs.readFileSync("src/lib/openai.ts","utf8");const lines=code.split("\n");console.log("Total lines:",lines.length);for(let i=0;i<lines.length;i++)console.log((i+1).toString().padStart(4,"0"),lines[i]);}catch(e){console.log("NOT FOUND, trying .js");try{const code=fs.readFileSync("src/lib/openai.js","utf8");console.log(code);}catch(e2){console.log("openai lib NOT FOUND");}}
