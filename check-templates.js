const lines=require("fs").readFileSync("src/components/editor/panel-editor.tsx","utf8").split("\n");for(let i=0;i<lines.length;i++){if(lines[i].includes("DesignTemplate")||lines[i].includes("templates")||lines[i].includes("applyTemplate")){if(lines[i].includes("interface")||lines[i].includes("const templates")||lines[i].includes("applyTemplate")||lines[i].includes("type DesignTemplate"))console.log((i+1).toString().padStart(4,"0"),lines[i].substring(0,180));}}
