const lines=require("fs").readFileSync("src/components/editor/panel-editor.tsx","utf8").split("\n");const terms=["Save","Load","Templates","Copy","Review","Layers","History"];terms.forEach(term=>{for(let i=0;i<lines.length;i++){if(lines[i].includes(">"+term+"<")||lines[i].includes("> "+term+" <")||lines[i].includes("\""+term+"\"")||lines[i].includes(">"+term+"</"))console.log(term+" at line "+(i+1)+": "+lines[i].substring(0,150));}});
